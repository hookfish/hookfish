{
  "name": "@hookfish/example-hono-node",
  "private": true,
  "version": "0.0.0",
  "license": "SEE LICENSE IN LICENSE",
  "type": "module",
  "scripts": {
    "dev": "node --watch --env-file-if-exists=../../../apps/frontend/.env src/index.ts",
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "verify-runtime": "node scripts/verify-runtime.mjs"
  },
  "dependencies": {
    "@hono/node-server": "^2.0.12",
    "@hookfish/api": "workspace:*",
    "@hookfish/database": "workspace:*",
    "@hookfish/providers": "workspace:*",
    "hono": "^4.12.34"
  },
  "devDependencies": {
    "@types/node": "^24.12.3",
    "typescript": "~5.9.3"
  }
}
