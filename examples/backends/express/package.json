{
  "name": "@hookfish/example-express",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node --watch --env-file-if-exists=../../../apps/frontend/.env --import tsx src/index.ts",
    "build": "tsc -b",
    "typecheck": "tsc -b"
  },
  "dependencies": {
    "@hono/node-server": "^2.0.12",
    "@hookfish/api": "workspace:*",
    "@hookfish/database": "workspace:*",
    "@hookfish/providers": "workspace:*",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^24.12.3",
    "tsx": "^4.23.1",
    "typescript": "~5.9.3"
  }
}
