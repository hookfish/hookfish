---
'@hookfish/api': minor
'@hookfish/hooks': minor
'@hookfish/provider': minor
---

Add lazy provider sources with per-provider resolution and optional flexible listings that accept arbitrary query parameters and pass pagination metadata through.
